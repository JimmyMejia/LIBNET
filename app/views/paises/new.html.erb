<h1>Nuevo Pais</h1>
<%= form_for :Pais, url: paises_path do |f| %>
<div class="field">
	<%= f.label :pais %>
	<%= f.text_field :pais, {:size => "20", :maxlength => "30"} %>
</div>

<div class="actions">
	<%= f.submit 'Guardar' %>
</div>

<% end %>
