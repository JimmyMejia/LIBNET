<%= form_for :tipomaterial, url: tipomateriales_path do |f| %>
	<% if @nuevo_tipomaterial.errors.any? %>
		<div id="alert alert-danger">
			<h2><%= pluralize(@nuevo_tipomaterial.errors.count,"error")%> antes de guardar:</h2>
			<ul>
				<% @nuevo_tipomaterial.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="control-group">
		<%= f.label :descripción, class: "control-label" %>
		<div class="controls">		
			<%= f.text_field :descripcion, class: "input-xlarge", required: true, placeholder: "Descripción de material" %>
		</div>
	</div>

	<div class="control-group">
		<div class="controls">
			<%= f.submit 'Guardar', class: "btn btn-large btn-primary" %>
		</div>
	</div>

<% end %>