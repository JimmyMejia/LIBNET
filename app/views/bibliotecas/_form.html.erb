<%= form_for(@nueva_biblioteca) do |f| %>
<% if @nueva_biblioteca.errors.any? %>
	<div id="alert alert-danger">
      <h2><%= pluralize(@nueva_biblioteca.errors.count, "error") %> antes de guardar:</h2>
		<ul>
			<% @nueva_biblioteca.errors.full_messages.each do |message| %>
				<li><%= message %></li>
			<% end %>
		</ul>
	</div>
<% end %>

	<div class="control-group">
		<%= f.label :nombre, class: "control-label" %>
		<div class="controls">
			<%= f.text_field :nombre, class: "input-xlarge", required: true, placeholder: "Nombre de la biblioteca" %>	
		</div>
	</div>
	<div class="control-group">
		<%= f.label :dirección, class: "control-label" %>
		<div class="controls">
			<%= f.text_area :direccion, class: "input-xlarge", required: true, placeholder: "Dirección" %>
		</div>
	</div>
	<div class="control-group">
		<%= f.label :teléfono, class: "control-label" %>
		<div class="controls">
			<%= f.text_field :telefono, class: "input-xlarge", required: true, placeholder: "Teléfono" %>
		</div>
	</div>
	<div class="control-group">
		<%= f.label :email, class: "control-label" %>
		<div class="controls">
			<%= f.text_field :email, class: "input-xlarge", required: true, placeholder: "correo@servidordecorreo" %>
		</div>
	</div>
	<div class="control-group">
		<%= f.label :logo, class: "control-label" %>
		<div class="controls">
			<%= f.file_field :logo, class: "input-xlarge", placeholder: "Logo de la biblioteca" %>
		</div>
	</div>

	<div class="control-group">
		<div class="controls">
			<%= f.submit 'Guardar', class: "btn btn-large btn-primary"%>
		</div>
	</div>
<% end %>